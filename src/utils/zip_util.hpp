#ifndef RIKKI_PATCHER_UTILS_ZIP_UTIL_HPP
#define RIKKI_PATCHER_UTILS_ZIP_UTIL_HPP
#include "precompiled.hpp"

/*class ZipUtil {
public:
    static size_t zip(const std::vector<path_t>& src, const path_t& dst, bool withPw = false, std::string_view pw = "");
    static size_t unzip(const path_t& src, const path_t& dst, bool withPw = false, std::string_view pw = "");
};*/


#endif //RIKKI_PATCHER_UTILS_ZIP_UTIL_HPP
